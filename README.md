# Система управления инцидентами для автопарка

## Описание проекта

Это Spring Boot приложение представляет собой систему мониторинга и управления инцидентами для автопарка. Приложение позволяет отслеживать, регистрировать и управлять инцидентами, происходящими с автобусами, а также генерировать аналитические отчеты.

## 🚀 Быстрый старт за 5 минут

### 1. Клонирование проекта
```bash
git clone https://github.com/AbbisDaniyar/Laba-SP.git
cd Laba-SP
```

### 2. Создание конфигурационных файлов
```bash
# Основные файлы конфигурации (из примеров)
cp .env.example .env
cp docker-compose.yml.example docker-compose.yml

# Конфигурация Spring Boot приложения
cd demo
cp src/main/resources/application.yml.example src/main/resources/application.yml
cp src/main/resources/application-docker.yml.example src/main/resources/application-docker.yml
cd ..
```

### 3. Настройка переменных окружения
Отредактируйте `.env` файл:
```bash
nano .env
```

**Обязательные настройки:**
```bash
POSTGRES_PASSWORD=ваш_надежный_пароль
JWT_SECRET=$(./generate-secrets.sh)  # запустите скрипт для генерации
```

**Опциональные настройки (Telegram бот):**
```bash
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

### 4. Генерация JWT секрета
```bash
./generate-secrets.sh
# Скрипт сгенерирует JWT_SECRET и добавит его в .env файл
```

### 5. Запуск проекта
```bash
docker-compose up --build -d
```

### 6. Проверка работоспособности
```bash
# Проверить статус контейнеров
docker-compose ps

# Просмотреть логи
docker-compose logs -f app

# Проверить health приложения
curl http://localhost:8080/actuator/health
```

## 🌐 Доступ к сервисам

- **Веб-приложение**: http://localhost:8080
- **Swagger UI**: http://localhost:8080/swagger-ui.html
- **База данных**: localhost:5432 (логин: `postgres`, пароль из `.env`)
- **MinIO (файловое хранилище)**: http://localhost:9001 (логин: `minioadmin`, пароль: `minioadmin`)
- **Логи приложения**: `docker-compose logs -f app`
- **Логи базы данных**: `docker-compose logs -f postgres`

## 🛠 Основные команды

### Docker Compose
```bash
# Запуск
docker-compose up -d

# Пересборка и запуск
docker-compose up --build -d

# Остановка
docker-compose down

# Остановка с удалением данных
docker-compose down -v

# Просмотр логов
docker-compose logs -f app

# Перезапуск
docker-compose restart app
```

### Разработка
```bash
# Локальный запуск (без Docker)
cd demo
./mvnw spring-boot:run

# Сборка проекта
./mvnw clean package

# Тестирование
./mvnw test
```

## 📁 Структура файлов

### Файлы в репозитории (шаблоны)
```
📄 .env.example              # Пример переменных окружения
📄 docker-compose.yml.example # Пример Docker Compose конфигурации
📄 README.md                 # Эта документация
📄 generate-secrets.sh       # Скрипт генерации JWT секрета
📄 setup-config.sh           # Скрипт настройки конфигурации
```

### Локальные файлы (создаются пользователем)
```
📄 .env                     # Реальные переменные окружения (НЕ в git)
📄 docker-compose.yml       # Реальная Docker конфигурация (НЕ в git)
```

### Spring Boot конфигурации
```
demo/src/main/resources/
├── application.yml           # Основная конфигурация
├── application-docker.yml    # Конфигурация для Docker
└── application.yml.example   # Пример конфигурации
```

## 🏗 Архитектура приложения

Проект построен на основе архитектуры MVC (Model-View-Controller) с использованием следующих технологий:

- **Spring Boot 3.5.6** - основной фреймворк
- **Java 17** - язык программирования
- **PostgreSQL** - основная база данных
- **MinIO** - файловое хранилище
- **H2** - встроенная база данных для тестирования
- **Spring Security** - аутентификация и авторизация
- **JWT** - токены для аутентификации
- **Lombok** - автоматическая генерация boilerplate кода
- **Hibernate/JPA** - ORM для работы с базой данных
- **Apache Commons CSV** - работа с CSV файлами
- **iText 7** - генерация PDF отчетов
- **Swagger/OpenAPI** - документация API
- **Telegram Bot API** - отправка уведомлений
- **Caffeine** - кэширование данных

## 🎯 Функциональные возможности

### 1. Управление инцидентами
- Создание, чтение, обновление и удаление инцидентов
- Фильтрация инцидентов по статусу, автобусу и пользователю
- Назначение инцидентов пользователям
- Изменение статуса инцидентов (NEW, IN_PROGRESS, RESOLVED)

### 2. Управление автобусами
- Создание, чтение, обновление и удаление автобусов
- Поиск автобусов по модели
- Управление автобусами с разными уровнями доступа

### 3. Аутентификация и авторизация
- JWT-аутентификация с использованием access и refresh токенов
- Три уровня ролей: USER, MANAGER, ADMIN
- Возможность смены пароля
- Интеграция с Telegram для уведомлений об аутентификации

### 4. Работа с файлами
- Загрузка файлов к инцидентам
- Хранение файлов в MinIO
- Возможность скачивания файлов
- Валидация загружаемых файлов

### 5. Импорт данных
- Импорт инцидентов из CSV файлов
- Валидация данных при импорте
- Подробная статистика об успешных и неудачных импортах

### 6. Генерация отчетов
- Ежедневные, еженедельные и ежемесячные отчеты
- Пользовательские отчеты с фильтрацией
- Генерация PDF-документов с подробной статистикой
- Таблицы и графики в отчетах

### 7. Кэширование
- Кэширование данных для повышения производительности
- Автоматическая инвалидация кэша при изменении данных
- Поддержка различных типов кэшей (инциденты, пользователи, автобусы)

## 📡 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/refresh` - Обновление токена
- `POST /api/auth/logout` - Выход из системы
- `GET /api/auth/info` - Информация о текущем пользователе
- `PUT /api/auth/change_password` - Смена пароля

### Инциденты
- `GET /api/alerts` - Получить все инциденты
- `GET /api/alerts/{id}` - Получить инцидент по ID
- `GET /api/alerts/bus/{busId}` - Получить инциденты по ID автобуса
- `GET /api/alerts/user/{userId}` - Получить инциденты по ID пользователя
- `POST /api/alerts` - Создать новый инцидент
- `PUT /api/alerts/{id}/status` - Обновить статус инцидента
- `PUT /api/alerts/{id}/assign` - Назначить инцидент пользователю
- `DELETE /api/alerts/{id}` - Удалить инцидент
- `POST /api/alerts/cache/clear` - Очистить кэш инцидентов
- `POST /api/alerts/import-csv` - Импорт инцидентов из CSV
- `POST /api/alerts/{id}/upload` - Загрузить файл к инциденту

### Автобусы
- `GET /api/buses` - Получить все автобусы
- `GET /api/buses/{id}` - Получить автобус по ID
- `GET /api/buses/search?model={model}` - Поиск автобусов по модели
- `POST /api/buses` - Создать новый автобус
- `PUT /api/buses/{id}` - Обновить автобус
- `DELETE /api/buses/{id}` - Удалить автобус

### Отчеты
- `GET /api/reports/pdf-modern/daily` - Ежедневный отчет
- `GET /api/reports/pdf-modern/weekly` - Еженедельный отчет
- `GET /api/reports/pdf-modern/monthly` - Ежемесячный отчет
- `POST /api/reports/pdf-modern/custom` - Пользовательский отчет
- `GET /api/reports/pdf-modern/statistics` - Статистика

### Файлы
- `GET /files/{filename}` - Скачивание файла
- `POST /api/alerts/{id}/upload` - Загрузка файла к инциденту

## 🔐 Безопасность

- Защита API с использованием JWT токенов
- Ролевая система доступа (RBAC)
- CORS настройки для безопасного взаимодействия с фронтендом
- Валидация входных данных
- Защита от CSRF атак (отключена для REST API)
- Проверка типов файлов при загрузке
- Ограничение размера загружаемых файлов

## 🧠 Кэширование

Приложение использует кэширование для повышения производительности:
- Инциденты (`alerts`, `alertsByStatus`, `alertsByBus`, `alertsByUser`)
- Пользователи (`userDetails`)
- Автобусы (`buses`, `bus`)

## 📢 Уведомления

- Интеграция с Telegram для отправки уведомлений
- Уведомления об аутентификации (успешные и неудачные попытки)
- Уведомления о важных событиях в системе
- Уведомления об ошибках аутентификации

## 🧪 Тестирование

- Полное покрытие юнит-тестами бизнес-логики
- Интеграционные тесты для контроллеров
- Использование H2 базы данных для тестирования
- Mockito для создания mock-объектов

## 🐳 Запуск в Docker

Приложение может быть запущено в Docker контейнерах с использованием docker-compose.

### Подготовка к запуску

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Отредактируйте файл `.env`, установив необходимые значения:
   - `POSTGRES_PASSWORD` - надежный пароль для PostgreSQL
   - `JWT_SECRET` - сгенерируйте новый секретный ключ (например, с помощью `openssl rand -base64 32`)
   - `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` (если планируете использовать Telegram уведомления)

3. Убедитесь, что у вас установлен Docker и Docker Compose

### Запуск приложения

Для запуска приложения выполните команду:

```bash
docker-compose up -d
```

Приложение будет доступно по адресу: `http://localhost:8080`

### Структура контейнеров

- `emergency-postgres` - PostgreSQL база данных
- `emergency-minio` - MinIO сервер для хранения файлов
- `emergency-app` - Spring Boot приложение

### Остановка приложения

Для остановки приложения выполните команду:

```bash
docker-compose down
```

Для удаления всех данных (включая базу данных):

```bash
docker-compose down -v
```

### Настройка переменных окружения

В файле `.env` можно настроить следующие параметры:

- `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD` - настройки базы данных
- `SERVER_PORT` - порт, на котором будет работать приложение
- `JWT_SECRET`, `JWT_EXPIRATION`, `JWT_REFRESH_EXPIRATION` - настройки JWT
- `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` - настройки Telegram бота
- `MINIO_ACCESS_KEY`, `MINIO_SECRET_KEY` - настройки MinIO
- `FILE_UPLOAD_DIR`, `MAX_FILE_SIZE` - настройки загрузки файлов
- `CORS_ALLOWED_ORIGINS` - разрешенные источники для CORS
- `LOGGING_LEVEL_ROOT`, `LOGGING_LEVEL_COM_EXAMPLE_DEMO` - уровни логирования

### Сборка образа вручную

Если необходимо пересобрать образ приложения, выполните:

```bash
docker-compose build app
```

Затем запустите приложение:

```bash
docker-compose up app
```

## 🔧 Конфигурация

### База данных (PostgreSQL)
```yaml
# В docker-compose.yml
POSTGRES_DB: emergencydb
POSTGRES_USER: postgres
POSTGRES_PASSWORD: ваш_пароль  # из .env
```

### Spring Boot приложение
```yaml
# В application.yml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/emergencydb
    username: postgres
    password: ${POSTGRES_PASSWORD}
```

### JWT аутентификация
```yaml
jwt:
  secret: ${JWT_SECRET}
  expiration: 900000        # 15 минут
  refresh-expiration: 604800000  # 7 дней
```

## 🔒 Безопасность

### Файлы, которые НЕЛЬЗЯ добавлять в git:
- `.env` - содержит пароли и секреты
- `docker-compose.yml` - может содержать пароли

### Генерация безопасных секретов
Всегда используйте скрипт для генерации JWT секрета:
```bash
./generate-secrets.sh
```

## 🐛 Устранение неполадок

### 1. Ошибка подключения к базе данных
```bash
# Проверить статус PostgreSQL
docker-compose ps postgres

# Проверить логи
docker-compose logs -f postgres

# Перезапустить БД
docker-compose restart postgres
```

### 2. Ошибка JWT аутентификации
```bash
# Проверить JWT_SECRET в .env
grep JWT_SECRET .env

# Перегенерировать секрет
./generate-secrets.sh
```

### 3. Очистка проекта
```bash
# Полная очистка
docker-compose down -v
docker system prune -a

# Пересборка
docker-compose up --build -d
```

### 4. Проверка портов
Убедитесь что порты 8080, 5432 и 9000 свободны:
```bash
sudo lsof -i :8080
sudo lsof -i :5432
sudo lsof -i :9000
```

## 📝 Миграция данных

### Резервное копирование базы данных
```bash
# Создать бекап
docker-compose exec postgres pg_dump -U postgres emergencydb > backup.sql

# Восстановить из бекапа
cat backup.sql | docker-compose exec -T postgres psql -U postgres emergencydb
```

*Проект разработан для лабораторных работ по системному программированию. Система управления чрезвычайными ситуациями в транспортной системе.*